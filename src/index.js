import React from 'react';
import ReactDOM from 'react-dom/client';
import AllRoute from './routes/routes.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AllRoute/>
  </React.StrictMode>
);

